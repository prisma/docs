{
  "ignorePatterns": [
    {
      "pattern": "^http://localhost.*"
    },
    {
      "pattern": "^/"
    },
    {
      "pattern": "^https://(dev|www).mysql.com.*"
    },
    {
      "pattern": "^https://(dash|www).cloudflare.com.*"
    }
  ]
}
