---
title: 'Microsoft SQL Server connection string'
metaTitle: 'Microsoft SQL Server connection string (Preview)'
metaDescription: ''
---

<TopBlock>

The Microsoft SQL Server connection string follows the [JDBC standard](https://docs.microsoft.com/en-us/sql/connect/jdbc/building-the-connection-url?view=sql-server-ver15)

The following example uses integrated security (Windows):

```
sqlserver://HOST:PORT;database=DATABASE;integratedsecurity=true
```

The following example uses SQL authentication (username and password):

```
sqlserver://HOST:PORT;database=DATABASE;user=USER;password=PASSWORD
```

> **Note**: If you are connectiong from MacOS, add `encrypt=DANGER_PLAINTEXT` to disable TLS to ease 

</TopBlock>


## Arguments

TODO

| Argument name      | Required | Default                | Description                                                                                                        |
| :----------------- | :------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------ |
| `database`<br />`initial catalog` | No       | `master`     | The database to connect to.  |
| `username`<br />`user`<br />`uid`<br />`userid`  | No, if you use integrated security       |                     | Maximum number of seconds to wait for a new connection                                                             |
| `password`<br />`pwd`   | No, if you use integrated security       |                     | Maximum number of seconds to wait until a single query terminates                                                  |
| `encrypt`          | No       | `prefer`               | Configures whether to use TLS, possible values: `prefer`, `disable`, `require`                                     |
| `Integrated Security`          | No       |                        | Path to the server certificate                                                                                     |
| `schema`      | No       |                        | Path to the PKCS12 certificate                                                                                     |
| `loginTimeout`<br />`connectTimeout`<br />`connectionTimeout`      | No       |                        | Password that was used to secure the PKCS12 file                                                                   |
| `socketTimeout`        | No       | `accept_invalid_certs` | Configures whether to check for missing values in the certificate                                                  |
| `isolationLevel`           | No       |                        | Points to a directory that contains a socket to be used for the connection                                         |

## Scenarios

### Using integrated security (Windows)

TODO

### Using SQL Browser to connect to a named instance

TODO