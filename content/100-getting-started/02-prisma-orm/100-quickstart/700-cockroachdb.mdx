---
title: 'Quickstart with CockroachDB'
metaTitle: 'Quickstart: Prisma ORM with CockroachDB (15 min)'
metaDescription: 'Get started with Prisma ORM and CockroachDB in 15 minutes.'
sidebar_custom_props: { badge: '15 min' }
---

# Quickstart: Prisma ORM with CockroachDB

Get up and running with Prisma ORM and CockroachDB in under 15 minutes.

## Prerequisites

- [Node.js](https://nodejs.org/en/) installed
- A [CockroachDB](https://www.cockroachlabs.com/) database
- Basic knowledge of JavaScript or TypeScript

## Setup

```terminal
npm install prisma --save-dev
npm install @prisma/client @prisma/adapter-pg
```

## Schema Configuration

```prisma file=prisma/schema.prisma
generator client {
  provider   = "prisma-client"
  output     = "../generated/prisma"
  engineType = "client"
}

datasource db {
  provider = "cockroachdb"
  url      = env("DATABASE_URL")
}
```

## Connection String

```env file=.env
DATABASE_URL="postgresql://username:password@host:26257/mydb?sslmode=require"
```

## Next steps

- Learn about [CockroachDB features](/orm/overview/databases/cockroachdb)
- Explore [framework guides](/guides)
