---
title: 'Connect your database'
metaTitle: 'Connect your database'
metaDescription: 'Connect your database to your project'
langSwitcher: ['typescript', 'node']
dbSwitcher: ['mongodb', 'atlas']
toc: false
---

To connect your database, you need to set the `url` field of the `datasource` block in your Prisma schema to your database [connection URL](/reference/database-reference/connection-urls):

```prisma file=prisma/schema.prisma
datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}
```

Typically, the `url` is [set via an environment variable](/guides/development-environment/environment-variables) which is defined in the `.env` file:

<SwitchTech technologies={['*', 'mongodb']}>

```bash file=.env
DATABASE_URL="CONNECTION_STRING"
```

You now need to set `CONNECTION_STRING` to a [connection URL](/reference/database-reference/connection-urls) that points to your database.

The format of the connection URL for your database depends on the database you use. For MongoDB, it looks as follows (the parts spelled all-uppercased are _placeholders_ for your specific connection details):

```no-lines
mongodb://USERNAME:PASSWORD@HOST:PORT/DATABASE/
```

Here's a short explanation of each component:

- `USERNAME`: The name of your database user
- `PASSWORD`: The password for your database user
- `HOST`: The host where a [`mongod`](https://docs.mongodb.com/manual/reference/program/mongod/#mongodb-binary-bin.mongod)/[`mongos`](https://docs.mongodb.com/manual/reference/program/mongos/#mongodb-binary-bin.mongos) instance is running
- `PORT`: The port where your database server is running (typically `27017` for MongoDB)
- `DATABASE`: The name of the database

As an example, for a MongoDB database hosted at localhost, the [connection URL](/reference/database-reference/connection-urls) might look similar to this:

```bash file=.env
DATABASE_URL="mongodb://user:password@localhost:27017/myFirstDatabase"
```

</SwitchTech>

<SwitchTech technologies={['*', 'atlas']}>

```bash file=.env
DATABASE_URL="CONNECTION_STRING"
```

You now need to set `CONNECTION_STRING` to a [connection URL](/reference/database-reference/connection-urls) that points to your database.

The format of the connection URL for your database depends on the database you use. For MongoDB, it looks as follows (the parts spelled all-uppercased are _placeholders_ for your specific connection details):

```no-lines
mongodb+srv://USERNAME:PASSWORD@HOST/DATABASE
```

Here's a short explanation of each component:

- `USERNAME`: The name of your database user
- `PASSWORD`: The password for your database user
- `HOST`: The host where a [MongoDB Atlas](https://www.mongodb.com/cloud/atlas) deployment is running
- `DATABASE`: The name of the database

As an example, for a MongoDB database hosted on [MongoDB Atlas](https://www.mongodb.com/cloud/atlas), the [connection URL](/reference/database-reference/connection-urls) might look similar to this:

```bash file=.env
DATABASE_URL="mongodb+srv://user:password@cluster0.ns1yp.mongodb.net/myFirstDatabase"
```

</SwitchTech>

<SwitchTech technologies={['node', 'mongodb']}>

<NavigationLinksContainer>

<ButtonLink
  color="dark"
  type="primary"
  href="/getting-started/setup-prisma/start-from-scratch/mongodb-node-mongodb"
  arrowLeft
>
  Installation
</ButtonLink>

<ButtonLink
  color="dark"
  type="primary"
  href="./creating-the-prisma-schema-node-mongodb"
  arrow
>
  Creating the Prisma schema
</ButtonLink>

</NavigationLinksContainer>

</SwitchTech>

<SwitchTech technologies={['typescript', 'mongodb']}>

<NavigationLinksContainer>

<ButtonLink
  color="dark"
  type="primary"
  href="/getting-started/setup-prisma/start-from-scratch/mongodb-typescript-mongodb"
  arrowLeft
>
  Installation
</ButtonLink>

<ButtonLink
  color="dark"
  type="primary"
  href="./creating-the-prisma-schema-typescript-mongodb"
  arrow
>
  Creating the Prisma schema
</ButtonLink>

</NavigationLinksContainer>

</SwitchTech>
<SwitchTech technologies={['node', 'atlas']}>

<NavigationLinksContainer>

<ButtonLink
  color="dark"
  type="primary"
  href="/getting-started/setup-prisma/start-from-scratch/mongodb-node-atlas"
  arrowLeft
>
  Installation
</ButtonLink>

<ButtonLink
  color="dark"
  type="primary"
  href="./creating-the-prisma-schema-node-atlas"
  arrow
>
  Creating the Prisma schema
</ButtonLink>

</NavigationLinksContainer>

</SwitchTech>

<SwitchTech technologies={['typescript', 'atlas']}>

<NavigationLinksContainer>

<ButtonLink
  color="dark"
  type="primary"
  href="/getting-started/setup-prisma/start-from-scratch/mongodb-typescript-atlas"
  arrowLeft
>
  Installation
</ButtonLink>

<ButtonLink
  color="dark"
  type="primary"
  href="./creating-the-prisma-schema-typescript-atlas"
  arrow
>
  Creating the Prisma schema
</ButtonLink>

</NavigationLinksContainer>

</SwitchTech>
