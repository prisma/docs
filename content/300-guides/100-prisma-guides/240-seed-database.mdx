---
title: Seeding your database with Prisma Client
metaDescription: Learn how to seed your database using Prisma Client
---

<TopBlock>

This guide describes how to seed your database using Prisma Client. This makes it really easy to ensure you can replay the necessary data in your database as often as needed. You may want to do this for multiple reasons:

* To populate databases with the initial set of data necessary for your application to run
* During development, so you have some basic data for validating and using your application. This is especially helpful when using Prisma Migrate, since your development database may sometimes be reset. 

</TopBlock>

## Seeding in TypeScript

If you are using TypeScript, follow these steps:

1. Create a new file `prisma/seeds.ts`
2. In the `prisma/seeds.ts` file, import Prisma Client, initialize it and create some records. Example:

    ```ts
    import { PrismaClient } from '@prisma/client'

    const prisma = new PrismaClient()

    async function main() {
    const alice = await prisma.user.upsert({
        where: { email: 'alice@prisma.io' },
        update: {},
        create: {
        email: `alice@prisma.io`,
        name: 'Alice',
        posts: {
            create: {
            title: 'Check out Prisma with Next.js',
            content: 'https://www.prisma.io/nextjs',
            published: true,
            },
        },
        },
    })

    const bob = await prisma.user.upsert({
        where: { email: 'bob@prisma.io' },
        update: {},
        create: {
        email: `bob@prisma.io`,
        name: 'Bob',
        posts: {
            create: [
            {
                title: 'Follow Prisma on Twitter',
                content: 'https://twitter.com/prisma',
                published: true,
            },
            {
                title: 'Follow Nexus on Twitter',
                content: 'https://twitter.com/nexusgql',
                published: true,
            },
            ],
        },
        },
    })
    console.log({ alice, bob })
    }

    main()
    .catch((e) => {
        console.error(e)
        process.exit(1)
    })
    .finally(async () => {
        await prisma.$disconnect()
    })
    ```

3. Add a new script to your project's `package.json`:

    ```json
    "scripts": {
        "seed": "ts-node ./prisma/seeds.ts"
    },
    ```

4. To seed the database, run the npm script

    ```
    npm run seed
    ```

## Seeding in JavaScript

If you are using JavaScript, follow these steps:

1. Create a new file `prisma/seeds.js`
2. In the `prisma/seeds.js` file, import Prisma Client, initialize it and create some records. Example:

    ```js
    const { PrismaClient } = require('@prisma/client')

    const prisma = new PrismaClient()

    async function main() {
    const alice = await prisma.user.upsert({
        where: { email: 'alice@prisma.io' },
        update: {},
        create: {
        email: `alice@prisma.io`,
        name: 'Alice',
        posts: {
            create: {
            title: 'Check out Prisma with Next.js',
            content: 'https://www.prisma.io/nextjs',
            published: true,
            },
        },
        },
    })

    const bob = await prisma.user.upsert({
        where: { email: 'bob@prisma.io' },
        update: {},
        create: {
        email: `bob@prisma.io`,
        name: 'Bob',
        posts: {
            create: [
            {
                title: 'Follow Prisma on Twitter',
                content: 'https://twitter.com/prisma',
                published: true,
            },
            {
                title: 'Follow Nexus on Twitter',
                content: 'https://twitter.com/nexusgql',
                published: true,
            },
            ],
        },
        },
    })
    console.log({ alice, bob })
    }

    main()
    .catch((e) => {
        console.error(e)
        process.exit(1)
    })
    .finally(async () => {
        await prisma.$disconnect()
    })
    ```

3. Add a new script to your project's `package.json`:

    ```json
    "scripts": {
        "seed": "node ./prisma/seeds.js"
    },
    ```

4. To seed the database, run the npm script

    ```
    npm run seed
    ```